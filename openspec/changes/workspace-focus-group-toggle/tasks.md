## 1. 设置与数据模型

- [x] 1.1 在插件设置中新增 3 个工作区配置结构（emoji、hover 文案、绑定规则）与激活状态字段
- [x] 1.2 实现设置读取/写入与默认值初始化，确保重启后保持配置

## 2. UI 入口与交互

- [x] 2.1 在文件浏览器标题栏附近渲染 3 个右对齐按钮，样式对齐现有隐藏/显示按钮
- [x] 2.2 为每个按钮绑定 hover 文案与 emoji 展示
- [x] 2.3 实现按钮激活态样式与互斥逻辑（同一时刻仅一个激活）

## 3. 批量隐藏/恢复逻辑

- [x] 3.1 记录激活前可见性快照（以路径为 key），并在激活时批量应用隐藏/显示
- [x] 3.2 点击已激活按钮时恢复快照并清除激活态
- [x] 3.3 切换到其他工作区时，先恢复旧快照，再应用新规则

## 4. 可靠性与性能

- [x] 4.1 对批量隐藏/显示做合并更新，避免逐项渲染抖动
- [x] 4.2 处理激活期间文件变更导致的快照失效（忽略不存在项）
